defmodule Loupe.Stream.ComparatorTest do
  use Loupe.TestCase, async: true

  describe "compare/3" do
    test "compares an operation with an item" do
      raise "Impl me siouplait"
    end
  end
end
